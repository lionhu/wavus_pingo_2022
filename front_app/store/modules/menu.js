import Menu from '../../data/menu'

 const state = {
  data: Menu.data,
  dashboardnav:Menu.dashboard
};

// getters
 const getters = {
};

// mutations
 const mutations = {
};

// actions
 const actions = {
};
export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
